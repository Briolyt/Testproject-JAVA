name: Run project test

on: push 
jobs:
  test:
    runs-on: ubuntu-latest

    steps:
    - name: Checkout this repo
      uses: actions/checkout@v2
      with:
        ref: ${{ github.ref }}

    - name: Run tests
      run: mvn —batch-mode clean install -f initial/pom.xml -Psonar-coverage -DskipTests -Ddbtype=postgresql

